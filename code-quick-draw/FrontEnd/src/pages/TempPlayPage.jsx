import Lobby from "../components/Lobby";
import {Header} from "../components/Header"

export const TempPlayPage = () => {
    return (
        <>
            <Header></Header>
            <Lobby />
        </>
    )
}